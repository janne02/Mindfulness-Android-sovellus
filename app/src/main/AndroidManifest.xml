<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekti">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projekti">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".MoodNegative" />
        <activity android:name=".MoodNeutral" />
        <activity android:name=".MoodPositive" />
        <activity android:name=".MeterActivity" />
        <activity android:name=".OmatTiedot" />
        <activity android:name=".ViimeinenAktiviteetti" />
        <activity android:name=".NeljasAktiviteetti" />
        <activity android:name=".KolmasAktiviteetti" />
        <activity android:name=".TokaAktiviteetti" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>